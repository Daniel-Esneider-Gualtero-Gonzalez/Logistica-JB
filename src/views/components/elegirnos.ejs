<style>
    #elegirnos * {
        transition: all 400ms linear;
    }

    #elegirnos .line {
        width: 1px;
        height: 15px;
    }

    #question-elegirnos:hover {
        background-color: blue;
        color: white;
    }
</style>


<div id="elegirnos" class=" flex flex-col">
    <div id="question-elegirnos" class="w-fit mx-auto   rounded p-3">
        <h2>¿Porque elegirnos?</h2>
    </div>
    <span class="line  border mx-auto"></span>
    <hr class="line-div-concepts">
    <section id="container-concepts" class="sm:flex gap-2">

        <div class="flex flex-col">
            <span class="line  border mx-auto"></span>
            <span class="mx-auto  ">Precios</span>
            <span class="line  border mx-auto"></span>
            <p class="p-1 border rounded">Reprehenderit, cumque temporibus! Veritatis cupiditate minima nam facilis
                iusto in reprehenderit harum fugit temporibus voluptas dolorum itaque voluptatem enim, doloremque
                eligendi amet?</p>

        </div>

        <div class="flex flex-col">
            <span class="line  border mx-auto"></span>
            <span class="mx-auto  ">Cotizáciones</span>
            <span class="line  border mx-auto"></span>
            <p class="p-1 border rounded">Reprehenderit, cumque temporibus! Veritatis cupiditate minima nam facilis
                iusto in reprehenderit harum fugit temporibus voluptas dolorum itaque voluptatem enim, doloremque
                eligendi amet?</p>

        </div>

        <div class="flex flex-col">
            <span class="line   border mx-auto"></span>
            <span class="mx-auto  ">Calidad</span>
            <span class="line border mx-auto"></span>
            <p class="p-1 border rounded">Reprehenderit, cumque temporibus! Veritatis cupiditate minima nam facilis
                iusto in reprehenderit harum fugit temporibus voluptas dolorum itaque voluptatem enim, doloremque
                eligendi amet?</p>

        </div>





    </section>




</div>

<script>
    const containerOpinion = document.getElementById("elegirnos")
    const elementsToAnimate = [{ element: "div", isAll: false }]

    function handleIntersectionObserver(entries, observer){
        entries.forEach(entry => {
            // Verificar si el elemento está intersectando (visible en la pantalla)
            if (entry.isIntersecting) {
                // elemento que interciono
                const element = entry.target
                element.style.backgroundColor = "blue"
                element.style.color = "white"
                observer.disconnect()
            } 
        });
    }

    elementsToAnimate.forEach(elementToAnimate => {
        if (elementToAnimate.isAll) {
            const elements = containerOpinion.querySelectorAll(elementToAnimate.element)
            console.log("all element to animate",elements)
            elements.forEach(element=>{
                const observer = new IntersectionObserver(handleIntersectionObserver)
                observer.observe(element)
            })
        } else {
            const element = containerOpinion.querySelector(elementToAnimate.element)
            const observer = new IntersectionObserver(handleIntersectionObserver)
                observer.observe(element)

        }
    })


</script>